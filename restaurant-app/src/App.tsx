import React from 'react'; // Import React explicitly (good practice in TypeScript files)
import logo from './logo.svg';
import './App.css';

// Define the App component as a functional component that returns a JSX.Element
const App: React.FC = () => {
  return (
    <div className="App">
      <header className="App-header">
        <img src={logo} className="App-logo" alt="logo" />
        <p>
          Edit <code>src/App.tsx</code> and save to reload. {/* Changed the extension in the message */}
        </p>
        <a
          className="App-link"
          href="https://reactjs.org"
          target="_blank"
          rel="noopener noreferrer"
        >
          Learn React
        </a>
      </header>
    </div>
  );
}

export default App;
